--- a/browser/ui/webui/brave_web_ui_controller_factory.cc
+++ b/browser/ui/webui/brave_web_ui_controller_factory.cc

CHANGE 1 — Add include at the top with other UI headers
==========================================================
Find this block (around line 14):
    #include "brave/browser/ntp_background/view_counter_service_factory.h"

Add immediately after it:
    #include "brave/browser/ui/webui/browse_here/browse_here_ui.h"


CHANGE 2 — Register the WebUI host in GetWebUIFactoryFunction
==============================================================
Find the block inside GetWebUIFactoryFunction that begins:
    if (
      #if BUILDFLAG(IS_ANDROID) && BUILDFLAG(ENABLE_BRAVE_WALLET)
        (url.is_valid() && url.host() == kWalletPageHost) ||
      #elif !BUILDFLAG(IS_ANDROID)

Add ONE line at the very start of the condition (before all the other OR-ed hosts),
still inside the if(...) block, like this:

    if (
        // BrowseHere video player page
        url.host() == kBrowseHereHost ||
    #if BUILDFLAG(IS_ANDROID) && BUILDFLAG(ENABLE_BRAVE_WALLET)
        (url.is_valid() && url.host() == kWalletPageHost) ||
    ...

This registers BrowseHereUI through the existing NewWebUI factory.

Alternatively, if a dedicated factory entry is preferred, add before the
final `return nullptr;` at the end of GetWebUIFactoryFunction:

    if (url.host() == kBrowseHereHost) {
      return &NewWebUI<BrowseHereUI>;
    }


DIFF (unified format for reference):
=====================================
@@ GetWebUIFactoryFunction
+  if (url.host() == kBrowseHereHost) {
+    return &NewWebUI<BrowseHereUI>;
+  }
+
   if (
 #if BUILDFLAG(IS_ANDROID) && BUILDFLAG(ENABLE_BRAVE_WALLET)
       (url.is_valid() && url.host() == kWalletPageHost) ||
